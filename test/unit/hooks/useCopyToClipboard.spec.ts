import { mount } from '@vue/test-utils';
import { defineComponent } from 'vue';

describe('hooks/useCopyToClipboard', () => {
  it('test', () => {
    // TOOD: ..
    mount(
      defineComponent({
        template: `<template />`,
      }),
    );
  });
});
