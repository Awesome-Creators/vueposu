import { mount } from '@vue/test-utils';
import { defineComponent } from 'vue';

describe('hooks/useDynamicList.spec', () => {
  it('test', () => {
    // TOOD: ..
    mount(
      defineComponent({
        template: `<template />`,
      }),
    );
  });
});
